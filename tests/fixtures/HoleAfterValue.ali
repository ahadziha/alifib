@Type

Arrow <<= { s, t, arr : s -> t },

A <<= { x, y, f : x -> y },

@A
  (* `arr => ?` on an already-defined generator does nothing: no error, no hole. *)
  let H :: Arrow = [ s => x, t => y, arr => f, arr => ? ]
